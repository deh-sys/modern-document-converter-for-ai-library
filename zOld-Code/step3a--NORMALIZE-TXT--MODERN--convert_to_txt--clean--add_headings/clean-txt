#!/usr/bin/env bash
# Lightweight wrapper to run the OCR TXT cleaner without installation.
set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
python3 "${SCRIPT_DIR}/clean_txt.py" "$@"
